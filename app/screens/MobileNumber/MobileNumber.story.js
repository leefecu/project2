import React from 'react';

import { storiesOf } from '@storybook/react-native';

import MobileNumber from './MobileNumber';

storiesOf('Page | MobileNumber', module).add('default', () => <MobileNumber />);
